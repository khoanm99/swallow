import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swallows.koha',
  appName: 'swallows',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
